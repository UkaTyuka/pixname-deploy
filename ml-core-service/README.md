# 📸 Image Captioning API (BLIP)

**Image Captioning API на основе модели [BLIP](https://huggingface.co/Salesforce/blip-image-captioning-base)**, генерирующий описания к изображениям с последующим переводом на русский язык.

Поддерживает:
- Асинхронную обработку запросов
- Генерацию **нескольких вариантов описания**
- Перевод с английского на русский

---

## 🧠 Возможности

- Принимает URL изображения
- Генерирует **3 различных описания**
- Поддерживает асинхронность и параллелизм
- Работает как локальный API сервис
- Может использоваться как backend для Telegram-бота

---

## 📦 Технологии

- FastAPI — для создания REST API
- BLIP (`Salesforce/blip-image-captioning-large`) — генерация описаний
- Helsinki-NLP — перевод текста
- Uvicorn + workers — высокая производительность
- HTTPX — асинхронный клиент для внешних запросов

---

## 🚀 Как запустить

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/PixName/ml-core-service
cd ml-core-service
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

> Требуется Python 3.8+

### 3. Запустите сервер

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --workers n
```

> Сервис будет доступен по адресу: `http://localhost:8000`

---